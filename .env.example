# Lucia Agent Host - Environment Variables Template
#
# Instructions:
#   1. Copy this file to .env in the project root: cp .env.example .env
#   2. Edit .env with your actual configuration values
#   3. NEVER commit .env to git (it's in .gitignore)
#
# Variables marked [REQUIRED] must be set for the application to work.
# Variables marked [OPTIONAL] have sensible defaults and are used by
# development scripts or Aspire — not by docker-compose directly.
#

################################################################################
# REQUIRED — LLM PROVIDER
################################################################################

# Chat model connection string (used by the AgentHost for all LLM inference)
# Format: Endpoint=<url>;AccessKey=<key>;Model=<model>;Provider=<provider>
#
# OpenAI example:
ConnectionStrings__chat-model=Endpoint=https://api.openai.com/v1;AccessKey=sk-proj-YOUR_KEY;Model=gpt-4o;Provider=openai
#
# Ollama (local, no API key):
# ConnectionStrings__chat-model=Endpoint=http://ollama:11434;AccessKey=ollama;Model=llama3.2;Provider=ollama
#
# Azure OpenAI:
# ConnectionStrings__chat-model=Endpoint=https://YOUR_RESOURCE.openai.azure.com/;AccessKey=YOUR_KEY;Model=gpt-4-deployment;Provider=azureopenai

################################################################################
# REQUIRED — HOME ASSISTANT
################################################################################

# Home Assistant instance URL
# Local network: http://192.168.1.100:8123
# Docker network: http://homeassistant:8123
HomeAssistant__BaseUrl=http://homeassistant:8123

# Long-lived access token (generate at: HA → Profile → Long-Lived Access Tokens)
HomeAssistant__AccessToken=PLACEHOLDER_REPLACE_WITH_ACTUAL_TOKEN

################################################################################
# OPTIONAL — HOME ASSISTANT TUNING
################################################################################

# Connection timeout in seconds (default: 60)
# HomeAssistant__TimeoutSeconds=60

# Skip SSL certificate validation (default: true)
# HomeAssistant__ValidateSSL=true

################################################################################
# OPTIONAL — EMBEDDING MODEL (for semantic search)
################################################################################

# Required only if you enable semantic entity search.
# Azure OpenAI is recommended for embeddings support.
# ConnectionStrings__embedding-model=Endpoint=https://YOUR_RESOURCE.openai.azure.com/;AccessKey=YOUR_KEY;Model=text-embedding-3-small;Provider=azureopenai

################################################################################
# OPTIONAL — DEPLOYMENT MODE
################################################################################

# "standalone" (default): all agents run in the AgentHost process.
# "mesh": agents run as separate A2A containers (used by K8s / Aspire).
# Deployment__Mode=standalone

################################################################################
# OPTIONAL — DEVELOPMENT & SCRIPTS
################################################################################
# These are NOT used by docker-compose but may be referenced by Aspire,
# local development scripts, or the export snapshot script.

# Aspire environment tag
# LUCIA_ENV=Development

# Log level override (Debug, Information, Warning, Error, Critical)
# LUCIA_LOG_LEVEL=Information

# OTEL collector endpoint (Aspire provides its own in dev)
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# CORS allowed origins for dashboard dev server
# Cors__AllowedOrigins__0=http://localhost:5173

# HA snapshot export (used by scripts/Export-HomeAssistantSnapshot.ps1)
# HA_ENDPOINT=http://homeassistant:8123
# HA_TOKEN=YOUR_LONG_LIVED_TOKEN


# Example config for scripts/sync-lucia-to-ha.sh
# Copy to scripts/.env or set these in your shell before running the script.
#
# Required:
HA_REMOTE_HOST=192.168.1.198
#
# Path to the Home Assistant config directory on the remote host.
# - Docker/HA OS: usually /config
# - Core in a venv: often ~/.homeassistant (use full path, e.g. /home/pi/.homeassistant)
HA_REMOTE_CONFIG=/config
#
# Optional (defaults shown):
# HA_REMOTE_USER=root
# HA_SSH_PORT=22
# HA_SSH_KEY=/home/you/.ssh/ha_ed25519

# Ripley — Orchestration Log
**Date:** 2026-03-26T18:25  
**Agent:** Ripley (Lead / Eval Architect)  
**Duration:** 271s  
**Task:** Deep audit of LightAgentEvalTests

## Executive Summary
All 6 xUnit tests in LightAgentEvalTests are smoke tests using only `AssertHasTextResponse()` + LLM judge fallback. No assertions check actual tool calls, entity resolution, or state changes. Full per-test critique provided.

## Key Findings
- **Test Specificity:** VERY LOW across all 6 tests
- **Available Assertions:** `AssertToolCalled()` exists but NEVER used
- **Real Validation:** Only occurs in separate EvalHarness (TUI), not in CI
- **Tool Name Bug:** YAML expects `ControlLightsAsync`, models emit `ControlLights` → false failures
- **Model Data:** granite4:350m: 32–67% score; gemma3:270m: 18.2% (too small, zero tool calls)

## Test-by-Test Analysis

### 1. FindLight_SingleLight_ProducesResponse
- Prompt: "Turn on the kitchen light"
- Assert: `AssertHasTextResponse()` only
- **Gaps:** No tool call verification, no entity resolution check, no state change validation

### 2. FindLightsByArea_AreaRequest_ProducesResponse
- Prompt: "Turn off all the lights in the living room"
- Assert: Same pattern
- **Gaps:** No area-based search verification, no multi-light targeting check

### 3. GetLightState_StatusQuery_ProducesResponse
- Prompt: "What is the status of the hallway light?"
- Assert: Same pattern
- **Gaps:** No `GetLightState` call verification, no state info content check

### 4. DimLight_ProducesResponse (5 STT variants)
- Prompts: Exact + 4 STT corruption variants
- Assert: Same pattern
- **Gaps:** No brightness parameter verification, no entity resolution under STT noise

### 5. SetColor_IntentRecognized_ProducesResponse
- Prompt: "Set the kitchen lights to blue"
- Assert: Same pattern
- **Gaps:** No color parameter verification, no HA color format check

### 6. OutOfDomain_MusicRequest_StaysInDomain
- Prompt: "Play some jazz music in the living room"
- Assert: `AssertHasTextResponse()` (result discarded with `_`)
- **Gaps:** No tool-call blocking verification, no polite decline validation

## Recommendations
1. Add specific assertions using `AssertToolCalled()` to every test
2. Verify entity resolution to correct HA entity_id
3. Validate extracted parameters (brightness, color, state)
4. Create model comparison matrix (granite4, gemma3, gpt-4, claude)
5. Port high-value YAML scenarios into xUnit with full assertions
6. Fix tool name mismatch bug in harness validation
7. Add trace-to-scenario pipeline for real production data

## Status
✅ Complete — Full critique written with per-test method analysis and infrastructure gaps documented.

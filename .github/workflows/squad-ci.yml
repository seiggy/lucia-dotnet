name: Squad CI
# dotnet project — configure build/test commands below

on:
  pull_request:
    branches: [master, dev, preview, main, insider]
    types: [opened, synchronize, reopened]
  push:
    branches: [master, dev, insider]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Set up .NET
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6.0.0
        with:
          dotnet-version: '10.x'

      - name: Restore dependencies
        run: dotnet restore lucia-dotnet.slnx

      - name: Build
        run: dotnet build lucia-dotnet.slnx --no-restore --configuration Release

      - name: Test
        run: dotnet test lucia.Tests --no-build --configuration Release --filter "Category!=Eval&Category!=Integration&Category!=LiveEval" --logger "console;verbosity=normal"

      - name: Test voice benchmarks
        run: dotnet test lucia.VoiceBenchmarks.Tests --no-build --configuration Release --logger "console;verbosity=normal"

      - name: Test MongoDB speaker profiles
        run: dotnet test lucia.Tests --no-build --configuration Release --filter "FullyQualifiedName~MongoSpeakerProfileStoreTests" --logger "console;verbosity=normal"

      - name: Test PostgreSQL migrations
        run: dotnet test lucia.Tests --no-build --configuration Release --filter "FullyQualifiedName~PostgresMigrationRunnerTests" --logger "console;verbosity=normal"

      - name: Test EvalHarness (provider-free)
        run: dotnet test lucia.EvalHarness.Tests --no-build --configuration Release --logger "console;verbosity=normal"
